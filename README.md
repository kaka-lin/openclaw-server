# OpenClaw Server

## 快速開始

請按照以下步驟來初始化並運行 OpenClaw 容器：

```bash
# 1. 複製專案並進入目錄
git clone https://github.com/kaka-lin/openclaw-server.git
cd openclaw-server

# 2. 設定環境變數
cp .env.example .env

# 3. 建置並啟動容器
docker-compose up -d --build
```
